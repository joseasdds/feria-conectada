# Setup local — Feria Conectada

Requisitos:
- Python 3.11+
- PostgreSQL 14+ (o SQLite para pruebas rápidas)
- Git

## 1) Clonar y crear entorno
```bash
git clone <REPO_URL>
cd feria-conectada
python -m venv .venv
. .venv/Scripts/activate  # Windows
# source .venv/bin/activate  # macOS/Linux